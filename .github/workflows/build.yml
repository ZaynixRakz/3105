name: Build iOS IPA
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Build IPA Application
        run: |
          xcodebuild -project ThreeOneOSFive.xcodeproj -scheme 3105 -configuration Release archive -archivePath $PWD/build/3105.xcarchive CODE_SIGNING_ALLOWED=NO
          mkdir -p $PWD/build/Payload
          cp -r $PWD/build/3105.xcarchive/Products/Applications/*.app $PWD/build/Payload/
          cd $PWD/build
          zip -r ZaynixFile.ipa Payload
          
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZaynixFile_Application
          path: build/ZaynixFile.ipa
          
